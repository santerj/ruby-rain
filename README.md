# ruby-rain

A relaxing ascii-style rain animation for the terminal - an afternoon project as a way to learn basic Ruby.
It relies on ncurses for getting the terminal size, so operation on other than Linux systems is probably impossible right now.

# sudo apt install ruby
# ruby rain.rb

Exit with ctrl+c.
